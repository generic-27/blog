/** @type {import('./$types').EntryGenerator} */
export function entries() {
	return [{ slug: 'up_and_going' }, { slug: 'scopes_and_hoisting' }, { slug: 'closure' }];
}

export const prerender = true;
